FUNC void ds(void) {
  int idx;

  exec("log > MyValues.log");
  for (idx = 0; idx < 3000; idx++) {
   // printf ("%d\n",Ch1[idx]);
  }
  exec("log off");
}

FUNC void ds2(void) {
  int idx;

  exec("log > MyValues2.log");
  for (idx = 0; idx < 50; idx++) {
    printf ("%f\n",DotSum[idx]);
  }
  exec("log off");
}

FUNC void ds3(void) {
  int idx;

  exec("log > MV.log");
  for (idx = 0; idx < 800; idx++) {
    printf ("%d\n",hz[idx]);
  }
  exec("log off");
	
	exec("log > MV1.log");
  for (idx = 0; idx < 800; idx++) {
    printf ("%d\n",hz1[idx]);
  }
  exec("log off");
	
	exec("log > MV2.log");
  for (idx = 0; idx < 800; idx++) {
    printf ("%d\n",hz2[idx]);
  }
  exec("log off");
}